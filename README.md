# ⚡ Nexus Engine — In-Browser Web Crawler & Search Indexer

An algorithmic web crawler, search engine, and ranking indexer built entirely on the client-side.

## Technical Constraints & Architecture
To demonstrate strict system design and data structure mastery without relying on external backend servers (Node.js/Python), this project evolves through a strict frontend stack:
1. Pure Semantic HTML5
2. Vanilla CSS & Responsive Layouts
3. Vanilla ES6+ JavaScript (DOMParser, Asynchronous Queue loops, BFS Graph Traversal)
4. React (Client-side state & UI components)
5. Supabase / PostgreSQL (Relational Graph Edge mapping & `tsvector` Full-Text Search)

## Development Stage
* **Current Status:** Commit 1 — Root Repository Initialization & HTML Shell